/** @type {import('next').NextConfig} */
const nextConfig = {
     
}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ['localhost'], // Add domain names where your images will be served from
  },
};
